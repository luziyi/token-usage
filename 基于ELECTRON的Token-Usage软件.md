# 基于ELECTRON的Token-Usage软件

## 软件需求：

1. 能够解析Opencode的数据库，并从中获取session，时间，每个session用户和ai的对话内容（调用工具部分可以省略），使用的ai模型名称，模型供应商，token消耗，缓存命中情况，缓存命中下输入输出token和缓存未命中下输入输出token数量

2. 根据解析opencode得到的数据，根据litellm去计算消费金额，优先完全匹配供应商和模型名称，如果模型供应商没有匹配，则仅匹配模型

3. 根据用户需求，按照模型，按照日期，按照session分类显示token消耗和消费金额，token不需要以M为单位

4. 软件要实时更新数据，最快更新数据，做到实时变动

## 要求：

能够完全正确解析opencode.db的数据

有一个完整可用的软件版本

可以借用src-old中的部分代码

界面简洁美观